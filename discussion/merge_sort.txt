/**
 * Pseudocode:
 * 1) Sort the left half
 * 2) sort the right half
 * 3) merge the halves
 **/

[8, 4, 6, 2, 7, 3, 5, 1]
	Left: [8, 4, 6, 2]
		Left: [8, 4]
			Left: [8] -> single element array so it is already sorted
			Right: [4] -> single element array so it is already sorted

			Merge: [4, 8]
		Right: [6, 2]
			Left: [6] -> single element array so it is already sorted
			Right: [2] -> single element array so it is already sorted

			Merge: [2, 6]